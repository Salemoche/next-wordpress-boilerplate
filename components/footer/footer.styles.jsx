import styled from 'styled-components'
import { ContentStyles } from '../../styles/global-components.styles';

export const FooterStyles = styled(ContentStyles)`
    display: flex;
    justify-content: center;

    a {
        margin-left: 5px;
    }
`